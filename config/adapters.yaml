adapters:
  airtable:
    api_key_env: AIRTABLE_API_KEY
    base_id_env: AIRTABLE_BASE_ID
    default_view: ""
    # input_field_map:  # {your_column: canonical_name}
    #   Name: name
    #   Prompt: prompt
    #   Model: client
    # output_field_map:  # {canonical_name: your_column}
    #   response: Response
    #   step: Step
    # named:               # per-base overrides that inherit from this base config
    #   marketing:
    #     base_id_env: AIRTABLE_MARKETING_BASE_ID
    #     input_field_map:
    #       Task: name
    #       Instructions: prompt
    #   research:
    #     base_id_env: AIRTABLE_RESEARCH_BASE_ID
    #     default_view: "active"
  google_sheets:
    api_key_env: GOOGLE_SHEETS_API_KEY
